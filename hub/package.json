{
  "name": "ppomi-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "test": "node --test",
    "sync:catalog": "node ../scripts/sync-catalog.mjs",
    "deploy": "npm run sync:catalog && vercel deploy --prod --yes --scope muilyzz"
  },
  "dependencies": { "@vercel/kv": "^3.0.0" }
}
